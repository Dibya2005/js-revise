//select all ele and print them using a loop
let list=document.querySelectorAll("li")
list.forEach((val)=>{console.log(val.textContent)})
//what diff between inner texttext content and inner html
//when do we use inner text and text content